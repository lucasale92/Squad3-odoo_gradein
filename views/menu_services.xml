<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Menú de Servicios -->
        <menuitem id="menu_services"
            name="Services"
            action="action_gradein_order"
        />

        <menuitem id="menu_services_config"
            name="Configuration"
            parent="menu_services"
            action="action_gradein_order" 
           groups="group_services_manager"/>

           
        <!-- Elemento de menú (Menu item) -->
        <menuitem id="menu_action_gradein_order"
                  name="Order GradeIn"
                  parent="menu_services"
                  action="action_gradein_order"/>

    </data>
    
</odoo>